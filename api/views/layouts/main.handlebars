<!DOCTYPE html>
<html lang="pt-br">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://fonts.googleapis.com/css?family=Montserrat&display=swap" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="/style/style.css">
    <title>Crypto Dashboard</title>
</head>

<body>
    <!-- Barra de Navegação -->
    <nav class="navbar offset-1 navbar-expand-lg navbar-dark bg-dark" id="barra-de-pesquisa">
        <div class="container-fluid">
            <a class="navbar-brand" href="crypto" id="logo">Crypto Dashboard</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
                aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav mx-auto">
                    <li class="nav-item">
                        <a class="nav-link active" href="crypto">Início</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="favoritos">Favoritos</a>
                    </li>
                </ul>
                <form class="d-flex flex-column flex-md-row align-items-center justify-content-center" id="pesquisar">
                    <input class="form-control me-2 mb-2 mb-md-0" type="search" placeholder="Pesquisar criptomoeda..."
                        aria-label="Search" id="input-pesquisar">
                    <button class="btn d-flex align-items-center justify-content-center" type="submit">
                        <img class="img-fluid" src="/img/img-pesquisar.png" alt="Ícone de pesquisa" id="img-pesquisar">
                    </button>
                </form>
            </div>
        </div>
    </nav>

    <script>
        document.getElementById('pesquisar').addEventListener('submit', function (event) {
            event.preventDefault(); // Evita que o formulário seja enviado da maneira padrão

            const query = document.getElementById('input-pesquisar').value.trim();

            if (query) {
                // Redireciona para a rota com o parâmetro da criptomoeda pesquisada
                window.location.href = `/crypto?crypto=${encodeURIComponent(query)}`;
            } else {
                alert('Por favor, insira o nome de uma criptomoeda.');
            }
        });
    </script>

    {{{body}}}

    <!-- Scripts do Bootstrap (bundled com popper.js) -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
        crossorigin="anonymous"></script>
</body>

</html>